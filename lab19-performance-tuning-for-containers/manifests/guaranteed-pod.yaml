apiVersion: v1
kind: Pod
metadata:
  name: guaranteed-pod
  namespace: performance-lab
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: 500m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 512Mi
