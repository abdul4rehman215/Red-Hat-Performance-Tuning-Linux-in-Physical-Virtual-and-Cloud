apiVersion: v1
kind: Pod
metadata:
  name: burstable-pod
  namespace: performance-lab
spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
